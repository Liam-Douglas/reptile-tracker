# Reptile Tracker Requirements
# Python 3.6+ required

# Core dependencies (all included with Python standard library)
# tkinter - GUI framework (comes with Python)
# sqlite3 - Database (comes with Python)
# csv - CSV handling (comes with Python)
# datetime - Date/time handling (comes with Python)

# External dependencies
Pillow>=9.0.0  # For image handling (reptile photos and receipt scanning)
pytesseract>=0.3.10  # For OCR text extraction from receipt images

# Note: Tesseract OCR must be installed on the system
# Installation instructions:
# - Ubuntu/Debian: sudo apt-get install tesseract-ocr
# - macOS: brew install tesseract
# - Windows: Download installer from https://github.com/UB-Mannheim/tesseract/wiki

# Optional: For development
# pytest>=7.0.0  # For unit testing (if you want to add tests)
# black>=22.0.0  # For code formatting (if you want to contribute)